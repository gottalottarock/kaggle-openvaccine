version: '2'
services:

    nsp:
#        env_file: .env
        build:
            context: .
        image: nsp
        environment:
            - NSP=/nsp-lib
            - DATA_FILE=/input/train.json
            - NUM_PROCESSES=32
        volumes:
            - ../nsp-lib:/nsp-lib
            - ../nsp-result_ya_2:/result
            - ../data:/input
        working_dir: /result
